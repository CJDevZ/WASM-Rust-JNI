## WASM Rust JNI
### This project basically loads all the plugins

Building example for Windows 64 bit:\
``cargo build --target x86_64-pc-windows-msvc --release``

### Reminders:
- Make sure to add every added api to the linker, ok?
